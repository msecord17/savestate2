# UI

Reusable layout and primitives. Can import from `src/core` (types, API) and `src/design` (tokens).
**Core must never import from here.**

- Use design tokens only (no hex).
- Touch targets ≥ 44px for interactive elements.
- No hover-only affordances; always provide tap/click feedback.
